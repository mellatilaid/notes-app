import 'package:flutter/material.dart';

class VoicePlayerView extends StatelessWidget {
  const VoicePlayerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
