import 'package:flutter/material.dart';

class FolderSubNotesBody extends StatelessWidget {
  const FolderSubNotesBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sub Notes'),
    );
  }
}
