import 'package:flutter/material.dart';

class VoiceNotesViewBody extends StatelessWidget {
  const VoiceNotesViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('voice view'),
    );
  }
}
