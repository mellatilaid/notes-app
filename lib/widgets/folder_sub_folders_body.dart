import 'package:flutter/material.dart';

class FolderSubFoldersBody extends StatelessWidget {
  const FolderSubFoldersBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sub Folders'),
    );
  }
}
