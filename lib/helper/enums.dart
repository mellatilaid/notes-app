enum EditNote {
  editNoteView,
  editSubNoteView,
}

//this enum is used to define the source of reminder
//to add/remover from list baces on it
enum ReminderSource {
  soonReminders,
  futureReminders,
  passedReminders,
}
